<h2> Welcome to <%= current_user.username %>'s Profile Page. </h2>
<h3> Here's a list of all of the questions that <%= current_user.username %> has asked...<h3>

<% current_user.questions.each do |question| %>
  <h3><a href="/questions/<%= question.id %>"><%= question.title %></a></h3>
<% end %>

